import ProductTasksPage from "@/pages/product-tasks";

// Keep the legacy route compatible with the current manual product-task flow.
export default function DailyEarningsPage() {
  return <ProductTasksPage />;
}